# Rotate the touchscreen 90° CW so touch coordinates match the
# display output rotated to landscape (Raspberry Pi Touch Display 2,
# native 720×1280 portrait → 1280×720 landscape via labwc).
# [MESHSAT-577 / kiosk touch alignment, verified on tesseract 2026-04-19]
#
# CW, not CCW, since both V1 kits carry the panel flipped 180°: this
# is the pair of `wlr-randr --transform 270` in labwc-autostart, and
# it is what both kits run on disk — verified live 2026-09-08.
# [MESHSAT-987]
#
# LIBINPUT_CALIBRATION_MATRIX applies at the libinput layer before any
# compositor-level transform — more reliable than labwc's rc.xml
# <calibrationMatrix> which didn't take effect on labwc 0.7.1.
#
# Matrix `0 1 0 -1 0 1` represents the 2×3 affine:
#     [ 0  1  0 ]     x' = y
#     [-1  0  1 ]     y' = -x + 1
# i.e. 90° CW in normalised coordinates. Swap to `0 -1 1 1 0 0`
# if the panel is rotated the other direction (that was the value
# this file shipped before both kits were flipped), and try the
# literal-180° form `-1 0 1 0 -1 1` if touch lands mirrored.
ACTION=="add|change", KERNEL=="event[0-9]*", ENV{ID_INPUT_TOUCHSCREEN}=="1", ENV{LIBINPUT_CALIBRATION_MATRIX}="0 1 0 -1 0 1"
