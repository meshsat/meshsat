# MeshSat OOB host agent socket directory. Created at boot and kept for the
# life of the system so the bridge container's bind mount of the directory
# never points at a recreated inode. [MESHSAT-756]
d /run/meshsat-oob 0750 root root -
